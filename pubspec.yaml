name: clipboard
description: A super-power clipboard package for Flutter. True multi-format clipboard support (text, HTML, images) with native platform channels, real-time monitoring, and production-grade reliability.
version: 3.0.5
homepage: https://github.com/samuelezedi/flutter_clipboard

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter